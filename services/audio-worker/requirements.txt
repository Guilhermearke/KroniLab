# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# Fila / storage / banco
redis==5.0.8
boto3==1.35.24          # Cloudflare R2 fala S3
supabase==2.7.4

# Audio
numpy==1.26.4
soundfile==0.12.1
librosa==0.10.2.post1
ffmpeg-python==0.2.0

# Separação de stems (BS-RoFormer via MSST)
# Instalar na imagem do worker (requer CUDA):
#
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   git clone https://github.com/ZFTurbo/Music-Source-Separation-Training msst
#   pip install -e msst
#   pip install ml_collections einops rotary-embedding-torch
#
# Para dev local sem GPU (usa MockEngine automaticamente):
#   nenhuma instalação extra necessária — o engine detecta a ausência e faz fallback

# Cloud fallback (Replicate)
# replicate==0.31.0   # descomentar se for usar ReplicateEngine
# requests==2.32.3    # idem
